# 同对手每日奖励限制验收（2026-09-21）

## 规则与实现

- 发放小王冠或 GG 粉尘前，复用 Mongo `gameresults` 查询双方当天的此前战绩；红蓝双方交换、普通匹配／密码房、投降／断线和非有效比赛均不能绕过。
- 钱包归属使用登录账号名，战绩查询使用服务端持有的双方显示名，与现有 `RedPlayerName`／`BluePlayerName` 的存储含义一致。
- 真人对局的战绩 `Id`、GG 回执 ID 和小局奖励 ID 前缀使用同一个服务端场次 ID，查询排除本场。已结束比赛的 GG 查询截止于本场结束时间，后续比赛记录不会反过来取消首场资格。
- 日期沿用现有“实际处理奖励时的中国日期（UTC+8）”。每次钱包 Revision 比较交换重试都重新检查日期和对手战绩，不把整场固定归属开局日期。跨日首次处理的旧事件仍按原处理日规则结算；已处理的事件跨日不能再领取。
- 被拦事件仍进入现有持久化去重账本，不增加奖励进度或粉尘。登录奖励独立，GG 社交提示／计数保留。不新增反刷集合或奖励计数模型；在现有战绩集合建立双方玩家名与时间的复合索引。
- 客户端每日任务页增加常驻两行说明，中／英／俄／波兰语同步到三个语言包目录。任务完成提示不会覆盖规则。

## 独立验收结果

| 检查 | 结果 |
| --- | --- |
| 服务端构建 | 通过，0 错误；原有 `Info.adminInfo` CS0649 警告 |
| `SameOpponentRewardsTest` | 38 项通过 |
| `DailyQuestTest` | 34 项通过 |
| `GGRewardsTest` | 54 项通过 |
| `PremiumCraftingTest` | 21 项通过 |
| `RewardClientTest` | 21 项通过 |
| 实际 SignalR／WebSocket 联网验收 | 39 项通过 |
| `scripts/Verify-Localization.py` | 通过；四语言、三处包一致，格式参数及引用键有效 |
| Unity 2019.4.41f2 实际画面 | 中文 1600×900／960×540，英／俄／波兰语 960×540；完成与未完成状态均检查，说明可见，无重叠截断 |

功能检查共 207 项。新规则测试含首场小局／双方 GG、后续同对手阻断、不同对手、红蓝互换、显示名与账号名不同、中国午夜的同毫秒记录、不完整断线战绩、延迟首场 GG、重复和并发、被拦事件跨日去重、服务重建，以及强制 Mongo Revision 冲突后跨日重新判定。

联网验收使用三个不同账号与不同显示名，经真实认证、匹配、投降结算、数据库战绩落库、旧签名 `SendGG`、通知及钱包查询。首场双方各得 5 粉尘，第二／第三场同对手不增加奖励，换新对手恢复奖励；旧 Common DLL 能反序列化响应。

## 复现与证据

- 原始构建／运行日志、隔离联网测试程序、语言检查及截图：`work/SameOpponentRewards/`。
- 新的持久化测试项目：`src/Cynthia.Card/test/SameOpponentRewardsTest/`。
- 测试使用独立 Mongo `127.0.0.1:28127` 和服务器 `127.0.0.1:5017`；正常本地服务器 `5005` 与数据库 `28020` 未重启或替换。
- 使用仓库自带 `work/LocalServer/dotnet/dotnet.exe`，构建时从 `work/LocalServer` 运行以使用固定 SDK；输出到 `work/SameOpponentRewards/build/<项目名>`。
- 新规则测试需设置 `REWARD_TEST_MONGO_URI=mongodb://127.0.0.1:28127`，并从 `src/Cynthia.Card/src/Cynthia.Card.Server` 工作目录运行测试 DLL，以读取服务端 `Locales` 资源。测试使用随机前缀数据，不删除现有账号或战绩。

边界：联网对局采用投降结束，用于验证真实匹配、保存战绩及 GG；没有人工打完两场完整真人牌局。小王冠限制在真实 Mongo 奖励路径验证，原有小局结算回归另行通过。UI 使用未登录账号的内存状态夹具渲染实际生产页面，不向账号发奖。没有部署线上服务器、生成发布客户端或提交／推送 Git。
