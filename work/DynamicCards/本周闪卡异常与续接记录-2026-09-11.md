# 闪卡异常、修复与续接记录（截至 2026-09-11）

用户已明确要求今天收尾、记录后停止。**下次收到用户明确恢复工作的指令后，再启动修复、测试或客户端构建。** 当前目标未完成，不应将“有画面变化”写成“完美闪卡已验收”。

工程：`C:/UnityProjects/LegacyGwent/src/Cynthia.Card.Unity/src/Cynthia.Unity.Card`，Unity 2019.4.1f1。

本次工作目录：`C:/UnityProjects/LegacyGwent/work/DynamicCards/CompleteMotionGoal-20260911`，下文简称 `W`。本记录汇总本线程本周闪卡工作；较早结果以项目 README 和相应结果文件为依据，没有在收尾时重新执行那些测试。

## 一、今天确认的异常与修复状态

| 异常 | 影响与证据 | 当前处理状态 |
|---|---|---|
| 后处理开关错误绑定到 Animator | 丹德里恩 `12220101` 的 `InitialFlash` 原本控制 `ForwardBloomSettings.m_Enabled`，转换后却控制根 Animator 开关，会影响主体动画。`script-binding-identities.json` 保存原始脚本身份。 | 已移除错误绑定，绑定到真正的后处理组件；已合入主工程，待修复后主界面复测。 |
| Hayate 开关错误绑定到粒子渲染器 | 尼斯里拉 `13221401` 的开关和 `deltaTime` 属于原始 Hayate；之前存在绑定到 ParticleSystemRenderer 的错误。 | 已按原脚本身份修正，随后处理修复导入；待主界面复测。 |
| 材质动画中间层遗漏 | 原始 Animator 参数经 OGOAnimationIntermediary 驱动材质，转换时没有转成可用的材质曲线。部分特效虽有时钟曲线，真正参数没有播放。 | 9 个场景补回 104 条有效曲线；保存曲线和实际 MaterialPropertyBlock 采样均通过。源 Shader 本身不存在的 3 个属性、4 条曲线没有伪造替代。 |
| 老粒子发射率字段不兼容 | Unity 2017 的 `EmissionModule.rate.scalar` 在 Unity 2019 中应映射到 `rateOverTime.scalar`；旧绑定不会正常驱动发射率。 | 4 个场景的 13 条曲线已修复，源值和实际属性采样通过。 |
| 原始 Hayate 粒子运动脚本未导入 | 噪声、湍流、吸引器等运动缺失，原来有粒子不代表运动方式完整。 | 44 个源场景、158 个组件已恢复；含金龙运行时生成的 TileA/TileC 子模板。逐字段与引用核对通过。 |
| 同名对象导致粒子脚本挂错对象 | 多个场景含同名粒子对象，仅按名字查找会覆盖或绑定到第一个对象。 | 改用原始对象身份与同名兄弟序号恢复；158 个组件配置复核无差异。 |
| 原始后处理渲染链缺失 | 原有 Bloom 和 Roach 的 Glitch 等配置没有连到实际卡牌相机。 | 恢复 11 个源效果组件、相关 13 条脚本曲线及相机后处理桥接；两套源 Shader 分开导入，待主界面复测。 |
| 蜡烛只做了简化替代 | 原始烛火的网格、图集选帧、UV、顶点噪声等没有完整恢复。 | 21 个原始组件已导入并移除旧替代烛火。Francis、Dorregaray 的隔离运行画面已检查；主界面修复后复测待做。 |
| 闪电只做了简化替代 | 原始分叉、网格形态、时序、宽度/透明度/材质动画和动画事件没有完整恢复。 | 8 个源场景、17 个组件、51 道闪电已恢复；Keira 的原始触发事件已接回，隔离运行已查看。 |
| 少量原始辅助行为遗漏 | 粒子朝向随速度调整，以及 Braenn 弓部跟随逻辑遗漏。 | 7 个 MeshParticleVelocityAlign、2 个 Braenn_BowFix 已恢复并合入。 |
| 变形怪烟雾的颜色曲线漏绑 | `Latest/16180101` 左右烟雾真实 Shader 有 `_TintColor`，原导入没有接回。 | 已补回 16 条曲线；源值与实际材质采样无差异。 |
| 昆恩法印存在源资源却漏映射 | 静态卡图留白影响旧图像匹配；`c10000100` 与原始 `Thronebreaker/16580100` 的画面完全对应。 | 已补上卡图关联；当前可映射覆盖为 651／671 种卡图。新增条目待主界面验证。 |
| 爱丽丝同伴一处循环标记不符源文件 | `13860101/Source_9e8a2ac9_VFXLoop.anim` 只有两条恒定材质偏移曲线，源文件不循环，工程此前设为循环。 | 已恢复 `m_LoopTime=0`；人物骨骼曲线未改。最终控制器和循环标记复核无不一致。 |

上表修复已经从隔离工程合入主工程，不再只是测试目录中的改动。但“已合入”与“修复后真实界面验收通过”仍须分开。

## 二、本周此前处理或核查过的问题

- **错误的素材版本回退**：终末之战 `11310100` 恢复 `Latest/10400101`；金龙 `11210700` 恢复 `Latest/10130101`。旧版替换曾丢失所需人物动作或出现火焰/地面异常。分别见 `RaghRepair-20260911`、`TrissVillenFire-20260911`。
- **狐妖云层黑方块**：同名材质实际是不同源对象，云粒子误用了整卡图集；改按具体材质资产绑定源云纹理。见 `AguaraRepair-20260911`。
- **特莉丝火焰长条/矩形边缘**：RGBA 通道保存动画帧，透明颜色扩张破坏了通道数据；恢复该纹理的原始导入行为。见 `TrissVillenFire-20260911`。
- **播放时序、片段身份和粒子事件**：此前修过依据片段名称猜循环、向空状态合成跳转、同名片段互相覆盖，以及粒子事件时刻计算等问题。范围和当时的包验证见 `TimingRepair`、`Assets/DynamicCards/README.md`，不要把历史包的验证直接套到本轮新包。
- **大小卡取景、长列表与离屏回收**：此前有大小卡裁切不一致、顶部覆盖及长列表回收问题，已有相应修复和当时实际 UI 检查。见 `MotionIntegrity/ResumeFraming-20260909/RESULTS.md`。
- **Roach / Iris / Vaedermakar 的源行为辨别**：已有独立进程播放核查。Roach 的马离开屋顶再返回属于源动作；Vaedermakar 的部分运动由 Shader 驱动，没有独立骨骼入场动画。见 `RoachVaederIris-20260911/RESULTS.md`。这些较早检查不是修复后主界面验收。

## 三、用户报告的“四张完全不动”目前能确认到哪里

本次在主 Unity 工程的真实卡组界面中检查了 Bekker's Dark Mirror、Dorregaray of Vole、Roach、Francis Bedlam；使用真实侧边 ArtCard，读取它实际生成的 RenderTexture，没有另造预览窗口或让记录器手动渲染。

- 修复前这四张卡在当前主工程中均观察到画面变化，包含持续播放和重新显示；Dorregaray、Roach、Francis 的蒙皮网格也有变化。证据：`W/reported-main-ui/results.json` 与截图。
- 因而，**用户截图所在入口的“完全静止”尚未被明确复现和归因**。不能据此说用户反馈不存在。
- 磁盘上现有独立客户端是 `Builds/Windows-20260908/DiyGwent.exe`，它携带旧资源。用户当时使用 Editor 还是独立客户端尚未确认；旧客户端可能相关，但不是已证实根因。
- 本轮后来确实找到了上述多类导入遗漏和错误绑定，已经修复。不能用四张有像素变化掩盖这些缺陷。

## 四、已经完成的检查，以及还没完成的验收

已完成：

1. 修复前真实主界面扫查全部 **671 种卡图**：650 种已映射且采样有画面变化，21 种没有映射。合并结果为 `W/all-main-ui-before-repairs.json`。它是修复前基线，不能改写成修复后的结果。
2. 原始绑定审计找到全部 1,671 个预期片段，检查到 287,461 条精确匹配的源绑定；305 条初筛属性疑点另行按原始组件、Shader 和代理逻辑核查。疑点不等于 305 个有效错误，也不能省略尚未完成的处置归档。
3. 本轮修复的 **91 个源场景**完成隔离 PlayMode/GPU 运行采样，无记录错误、无整卡空白；见 `W/stage-play/results.json`。这不等于 91 个场景全部通过主界面验收。
4. 修复曲线保存值与实际属性采样、Hayate 原始配置核对已通过，各专项 `*-report.json`、`hayate-source-config-verification.json` 有详细证据。
5. 合入后的最终源契约复核：1,042 个控制器、1,668 个片段的循环标记检查，无不一致、无缺失；见 `W/current-contract-summary.json`。不同审计的计数口径不同，不要与上面的绑定审计相加。
6. 主工程 C# 编译通过；第一轮 740 场景资源构建于 **2026-09-11 15:49:18 UTC** 完成，记录为 `W/main-bundle-build-first.txt`。包含最后两项修正的第二轮构建也已于 **2026-09-11 16:00:57 UTC（香港时间 9 月 12 日 00:00:57）** 自然完成，`editor-ready` 已写入。

尚未完成：

- 最终资源构建已完成，但独立的逐文件交付校验 `W/verify_main_delivery.py` 尚未运行。收尾快照见 `W/stop-state.json`；没有待执行的测试或构建请求。
- 修复后的真实主界面复测：**89 个卡牌条目只生成了清单，尚未启动**。清单是 `W/postmerge-ui-request.json`，不是自动执行队列 `W/request.json`。
- 91 个已修复源场景中，6 个当前没有游戏卡图入口：`12490100`、`13400100`、`13870100`、`14840100`、`11210701`、`11310101`；这些仅有隔离运行证据。
- 新的独立 Windows 客户端尚未构建，也没有替换 9 月 8 日客户端。
- **仍有 20 种卡图、22 个卡牌条目没有找到匹配动态源**。名单：`W/remaining-unmapped-after-source-repairs.json`；重新裁切后的三源比对：`W/cropped-rematch/candidates.json`、三张 `sheet-*.jpg`。不能用相似画面随意替代。
- 少量剩余属性疑点还需统一核对/汇总处置：尤其是“源 Shader 未公开同名属性”的记录，必要时还需核对实际 uniform/序列化字段，不能全部直接判为无效源曲线。
- Legacy 烛火 Shader 没有找到对应原始字节码，目前使用可获得的 Native 原始烛火 Shader；尚不能宣称与 Legacy 原版逐像素一致。

## 五、当前改动、备份及续接注意事项

主要合入：118 个已有文件（88 个 prefab、29 个动画、DynamicCardView）及 96 个新文件，合计 214 个文件，逐一比对合入前主工程和阶段工程哈希后复制并复核。清单：`W/main-merge-manifest.json`；备份：`W/MainMergeBackup`。

另有以下已记录的追加改动：

- `DynamicCardContentImporter.cs` 防止再次生成已经被原始组件替代的简化蜡烛/闪电，保留此前狐妖修复；备份为 `MainMergeBackup/DynamicCardContentImporter.before-guards.cs`。
- 昆恩目录关联和爱丽丝循环标记：`W/final-source-findings-applied.json` 含单独备份、前后哈希。
- 主工程还保留两个临时 Editor 工具：`Assets/Editor/CompleteMotionGameUiAudit.cs`、`Assets/Editor/CompleteMotionBuildHook.cs` 及其 `.meta`。它们要等下一次验收后清理；本次收尾不再触发编译。
- `W/CompleteMotionPlayerBuildHook.cs` 仅是准备好的工具文件，**未放入主工程、未运行**。
- 未提交、未暂存。已有本地化、字体、DOTween、包锁和 GraphicsSettings 等无关改动仍保留。部分 Content 与资源缓存被 Git 忽略，不能只靠 Git 工作树判断素材是否保存。
- **不要重新整目录覆盖隔离工程与主工程；不要未经哈希检查回滚；不要直接重跑全量旧导入器覆盖这次曲线修复。** 外部 `work/DynamicCards/SourceAnimationImporter.cs` 仍有按属性名宽泛匹配组件类型的潜在问题，若要再次运行全量导入，先处理其类型与原脚本身份匹配。

## 六、下次从这里继续

1. 先读本文件及 `W/stop-state.json`，核对 `main-bundle-build.txt`、`cards.bundle.editor-ready`、当前工程修改；不要默认构建已完成或再跑整轮构建。
2. 构建成功后运行已准备的 `W/verify_main_delivery.py`，核对这次改动的源文件、缓存清单、740 场景索引和全部发布资源包；若失败先检查差异，不能覆盖掉现有工作。
3. 在主 Unity 工程进入真实卡组界面，按 `W/postmerge-ui-request.json` 启动 **89 条目**复测。记录器增加了原始特效组件与不支持 Shader 的记录；检查日志、画面、人物动作和入场/循环，优先看用户四张牌、丹德里恩、尼斯里拉、蜡烛/闪电和新找回的昆恩法印。
4. 整理仍存的属性疑点及 Shader 来源限制；若发现真实遗漏，再按源组件/原曲线精确修复。
5. 如继续处理独立客户端，构建到新目录 `Builds/Windows-20260911`，保留旧客户端。打包时启用动态卡资源；自定义本地化目录 `Assets/StreamingFile` 需要随客户端带入 `DiyGwent_Data/StreamingFile`。现有 Bootstrapper 只在 Editor 选本地服务，独立客户端默认走远程域名；本地客户端测试路径尚未实现，不要误把它当作已支持。
6. 完成实际界面与交付验证后，再移除临时工具、完成文档和最终交付。到这一步之前，目标保持未完成。
