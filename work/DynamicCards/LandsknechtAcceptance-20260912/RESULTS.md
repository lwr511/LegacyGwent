# 2026-09-12 闪卡旋转修复收尾

用户于 2026-09-12 17:40 要求本轮收尾并提交一次。本轮在此结束，后续全量界面验收等待用户明确恢复。此次停止来自明确的收尾要求；Esc 仍仅表示停止当下电脑操作。

## 已完成

- 873 个动画片段中的 13,098 条骨骼四元数轨迹已修正插值，涉及 628 个源场景；保留关键帧数值、时间、其他曲线、原始 Euler 旋转顺序和资源 GUID。
- 873 个原动画及 `.meta` 已逐文件备份，修复已合入主工程并由 Unity 重新导入。完整源动画、其他变换及骨骼绑定核对见离线验收记录。
- 主工程动态资源包于 09:18:07 UTC 完成重建。873 个修复动画与元数据通过交付哈希核对，740 个源场景全部进入 26 个分包；包括目录文件在内的 28 个实际交付文件已记录 SHA-256。见 `post-rotation-delivery-verification.json`。
- 莱里亚长矛兵 `70104 / 202156 / 15210100` 已在真实组牌界面记录 431 帧、约 17 秒。前景 `_3_man` 网格使用的 43 条骨骼旋转轨迹全部匹配源动画，未调整时钟时最大误差为 **0.009710°**，覆盖两个完整的 8 秒循环。这是修复后主界面的记录，不是独立测试场景的结果。
- 贝克尔的黑暗之镜、多瑞加雷、萝卜、乞丐王分别完成四次主界面采样和一次重新打开检查。画面可见、人物网格有运动，未记录不支持的 Shader。萝卜的入场雪花效果有原始 `Roach_Intro_Effect / Glitch` 配置和曲线依据，马出现及离开屋顶的动作在本轮画面中可见。
- 全量界面批次完成 `12001` 至 `12010` 的四次采样；`12011` 仅完成两次采样，之后预览资源发生变化，未计作完成。加上上述四张牌及长矛兵，共有 **15 个不同卡面的完整本轮采样**，不代表全部卡牌已验收。

## 尚未完成与边界

- 671 个不同游戏卡面的逐卡验收尚未完成。后续从 `12011` 及其余未验收卡面继续，不能将批量采样或图像变化直接当作全部视觉正确。
- 20 种游戏卡图（涉及 22 个卡牌定义）仍无可用源素材映射，清单见离线记录。另有一项源骨骼缺失及 Fringilla 原始循环首尾姿态差异，需要按源限制说明，不能假造素材或改掉源动作。
- 记录器已增加资源身份检查、实际卡面图保存、预览中断后的有限重试和隐藏界面的等待逻辑。最终版本通过 Unity 自带 Roslyn 与本工程程序集的独立编译检查；新增中断重试逻辑没有在收尾后重新执行全量 PlayMode 验收。
- 编译检查通过，只有 `SourceAnimationImporter.cs` 中原有未使用变量的警告。此前真正的界面记录和资源包校验已完成；最后一次编译检查不操作 Unity 界面、不替换其已加载程序集。

## Git 与本地资源

按仓库现有规则，提取素材、动画文件、资源包、录屏、备份和测试工程均保留在本地且不进入 Git。本次提交保存修复/核验工具、不可变修复清单、交付校验与收尾记录。**仅拉取此提交不会取得新的 20 GB 动画素材或资源包**；换机仍需已有素材包以及本轮修复后的本地资源。本轮未更新云端素材包，也未重新发布 Windows 客户端。

关键本地路径（均相对本目录）：

- `BeforeRotationMerge/`：合入前原动画及元数据备份。
- `RepairStaging/`：经过核验的修复副本；不要重写历史 `rotation-repair-ready-manifest.json`。
- `post-rotation-enabled-main-man/`：长矛兵实际画面、骨骼记录、`actual-ui-motion.webp` 和源数据比较。
- `../CompleteMotionGoal-20260911/post-rotation-reported-four/`：四张问题牌的实际主界面记录。
- `../CompleteMotionGoal-20260911/post-rotation-all-main-ui/`：10 张完整采样及第 11 张的中断记录。
- 主资源包：工程 `Library/DynamicCardsBundles/StandaloneWindows64/`。

Unity 已退出 PlayMode，停在干净的 LoginScene；未留下待执行的采样请求。全量记录器的 `stop.txt` 保留本轮收尾原因。已有本地服务器保持运行。

修复入口和重现工具：`../SourceAnimationImporter.cs` 用于源动画导入；`BatchRotationRepair.cs` 是本轮实际使用的独立 Unity 修复程序，配合 `prepare_repair_jobs.py`、源导出数据及修复前文件使用；`merge_verified_rotation_repairs.py` 验证清单后在 Unity 关闭时合入。核验工具依赖本机已有的源素材与导出数据，不能只凭 Git 文件重建这些被排除的大资源。
