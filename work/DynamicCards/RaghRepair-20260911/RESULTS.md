# 终末之战（11310100）素材回退修复

历史 `d28bf804f` 的卡图 `11310100` 使用 `Latest/10400101`。`bba82a5fc`（2026-09-08）的旧版素材切换改为 `Legacy2017/11310101`，覆盖了带人物骨骼动画的版本。当前旧版实播可复现主体无骨骼动画、火焰方块和长条。

从 `BeforeOldSources-20260908` 恢复原先转换完成的 `Latest/10400101` 及缺失依赖共 40 个资源和配套 `.meta`，保留已有共享依赖。恢复原相机、声音、动画循环与手腕特效绑定。目录中旧场景保留为未映射条目，避免改变其余 Legacy2017 分包分组；唯一更换素材映射的卡图为 `11310100`。

`baseline-results.json` / `Baseline` 为旧版复现。`Prior/baseline-results.json` / `Prior/Baseline` 为恢复前对原先资源包的实播验证：大小卡预览均有两组控制器、持续骨骼和网格变化，播放超过一轮。`restore-plan.json` / `restore-applied.json` 记录依赖闭包及文件哈希；`catalog-before.json` 为目录备份。

本轮和狐妖云层修正共用构建。`build-progress.txt` 为真实构建日志，`build-result.txt` 出现 PASS 后才表示资源包和编辑器校验清单完成。最终实播将直接读取主工程缓存，结果在 `Delivered/baseline-results.json`，验证脚本为 `verify_delivery.py`；这些结果存在且通过前，不能把原先备份的测试视为本轮正式交付完成。

最终交付验证通过：2026-09-11 18:53:56（UTC+8）构建完成；独立 Unity 2019 进程直接读取主工程缓存，完成两张卡大小预览的 20 次采样，覆盖持续播放超过 18 秒及关闭后重开。终末之战骨骼和网格持续变化，狐妖两种云材质分别绑定正确纹理，实际画面无原报告的云层黑方块。86 个源文件/元数据与缓存清单哈希匹配；27 个发布文件共 1,743,396,631 字节，未涉及的分包哈希不变。临时主工程构建脚本已移除，未创建新游戏安装包。
